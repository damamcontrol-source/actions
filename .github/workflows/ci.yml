name: CI Demo

on:
  push:
    branches: [ main ]   # لو فرعك الأساسى master غيّرها لـ master
  workflow_dispatch:      # تشغيل يدوى من UI

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4   # الـ Action الرسمى

      - name: Hello World
        run: echo "Hello Mohammed from GitHub Actions 🚀"

